# continuum wire-fuzz case, format 1
target = frame.length-prefix
class = well-formed
expect = accepted
origin = seed
note = a two-byte payload under its own four-byte length prefix
bytes =
  000000027b7d

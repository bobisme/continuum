//! Fixture tests (inert data; never compiled).

/// Replays the committed fuzz corpus.
#[test]
fn replays_committed_corpus() {
    assert_eq!(1, 1);
}

#[test]
#[ignore]
fn ignored_marker() {
    assert_eq!(1, 1);
}

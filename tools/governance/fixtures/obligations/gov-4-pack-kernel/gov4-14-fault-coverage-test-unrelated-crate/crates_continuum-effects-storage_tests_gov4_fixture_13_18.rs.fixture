//! Fixture tests in an unrelated crate (inert data; never compiled).

#[test]
fn host_matches_lab() {
    assert_eq!(1, 1);
}

#[test]
fn crash_mid_write() {
    assert_eq!(1, 1);
}

#[test]
fn partitions_both_ways() {
    assert_eq!(1, 1);
}

//! Fixture tests (inert data; never compiled).

/// No-reduction differential campaign: continuum_engine_dpor vs continuum_engine_explicit.
#[test]
fn vs_unreduced() {
    let _ = (continuum_engine_dpor::VERSION, continuum_engine_explicit::VERSION);
}

/// Mutation: declare conflicting events independent.
#[test]
fn declares_conflicting_events_independent() {
    assert_eq!(1, 1);
}

#[test]
#[ignore]
fn ignored_marker() {
    assert_eq!(1, 1);
}

{
  "description": "`#[path]` in `continuum-certificate` loads a decoded-input producer from a file outside `src/**/*.rs`, which the line count, the C018 inventory and its digest would not see (Codex cr-10mg1y round 4).",
  "id": "kcov-07-certificate-path-module",
  "requirement": "KCOV-07",
  "rule": "no-plugins-or-dynamic-loading",
  "substitute": {
    "crates/continuum-certificate/src/lib.rs": [
      {
        "find": "pub mod check;",
        "replace": "pub mod check;\n#[path = \"hidden.inc\"]\nmod hidden;\npub use hidden::decide;"
      }
    ]
  }
}

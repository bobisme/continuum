{
  "description": "positive_re_encoding_a_decoded_value_reproduces_its_bytes is entirely absent from the (synthetic) source",
  "requirement": "TEST-3-09",
  "rule": "serialization-round-trip-rust-binding-missing",
  "system": {
    "kind": "round-trip-rust-binding",
    "source": "#[test]\nfn positive_decoding_an_encoding_returns_the_value() {\n    let plan = Plan::new(\"decode(encode(v)) == v\", 0x2021_0221_0000_0001, CASES);\n}\n"
  }
}

{
  "id": "tcb02-cfg-attr-path-redirect",
  "crate": "continuum-certificate",
  "why": "`cfg_attr(all(), path = \u2026)` with the dummy standard module file present sends rustc to a file outside src/ (Codex cr-3i3rst)",
  "edits": [
    {
      "file": "crates/continuum-certificate/src/lib.rs",
      "append": "\n#[cfg_attr(all(), path = \"../hidden/decide.rs\")]\nmod m;\n/// Fixture: re-exported from a file rustc reads and the scan does not.\npub use m::decide;\n"
    },
    {
      "file": "crates/continuum-certificate/src/m.rs",
      "append": "//! Fixture: the standard module file, which rustc never reads.\n"
    },
    {
      "file": "crates/continuum-certificate/hidden/decide.rs",
      "append": "/// Fixture: a non-wire verdict entry outside src/.\n#[must_use]\npub fn decide(decoded: &continuum_kernel_core::wire::Certificate) -> crate::Outcome {\n    let _ = decoded;\n    crate::check_certificate(&[])\n}\n"
    }
  ],
  "inventory_add": [
    "crates/continuum-certificate/src/lib.rs: use m::decide"
  ],
  "expect": "a module the scan cannot see",
  "expect_compiled_mismatch": true
}

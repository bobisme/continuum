# G0 Falsification Matrix — Revision 3

G0 exists to kill seductive architecture before it ossifies. A failed load-bearing spike blocks interface freeze.

The freeze-blocking subset is DX-01–03, 10, 12, 13, 14 (plan §22): a failed or unexecuted freeze-blocking item blocks interface freeze. Re-homed items block their target gates instead; DX-04, 05, 07, and 08 contributed Phase A artifact-shape spikes and are re-homed to the gates owning their engines (plan §22). Plan §0.3's counts derive from this table. Spike evidence (finite Python spikes, `spikes/R3_SPIKE_REPORT.md`) validates artifact shapes and interaction contracts, not engines, scale, concurrency, persistence, or soundness (docs/53). DX-09 requires preregistration before it runs: the three human-executed docs/34 acceptance workflows (the fourth, agent repair, is covered by the G2 ACI ablation and ContinuumBench), two cohorts (Rust newcomers on the deterministic/causal workflow; distributed-systems experts on real failures), a raw-trace baseline comparator, and cohort sizes, instruments, and pass thresholds fixed in an expanded docs/48 published before the study (plan §21.1).

| ID | Question | Required experiment | Pass condition | Failure consequence | Status | Evidence | Decision |
|---|---|---|---|---|---|---|---|
| G0-DX-01 | Can an agent diagnose from a bounded artifact rather than raw logs? | 200+ event noisy durability failure → Context Pack | causal core replay-preserving; ≥10× context reduction; exact expansion handles | redesign evidence model | Evidence (finite spike) | spikes/R3_SPIKE_REPORT.md §1 (Context Pack slicing) | adopt artifact shape; engine unproven (docs/53) |
| G0-DX-02 | Can Continuum detect formal reward hacking? | patches that weaken property, strengthen assumptions, reduce bounds, hide observer events, remove faults | all classified as intent changes; ordinary code guard repair is not | block autonomous repair | Evidence (finite spike) | spikes/R3_SPIKE_REPORT.md §2 (intent diff) | adopt artifact shape; engine unproven (docs/53) |
| G0-DX-03 | Is all state explicit and resumable? | create snapshot, start bounded task, resume continuation, mutate workspace, reuse old handle | idempotence, stale snapshot rejection, deterministic resume | reject daemon protocol | Evidence (reference implementation) | `crates/continuumd/tests/dx03_falsification.rs` is the reference-implementation evidence: 27 tests driven through `Daemon::dispatch` and nothing else — 22 adversarial attacks over four axes plus five `baseline_*` tests that re-run every scenario spikes/R3_SPIKE_REPORT.md §3 demonstrated (canonical snapshot identities, idempotent identical task creation, key-reuse rejection, resumable continuation, rejection of a continuation under a different snapshot), which remains the named baseline. Idempotence: keyed replay after six interleaved hostile operations, on a reconnected daemon (INV-002's state-transplant device), across a supersession with a fresh-key anti-vacuity control, and two fresh keys on one campaign. Stale-snapshot rejection: every snapshot-consuming operation (`verification.start`, `task.resume`, `workspace.fork`, `workspace.seal`, `workspace.diff`) against a superseded handle, the `LineageError::Unknown` arm after four advances, and a control isolating the idempotency ledger as the only guard. Deterministic resume: two fresh daemons compared frame for frame, a 6 hostile operations × 5 placements = 30-run interleaving sweep plus its un-interleaved control, and all five arms of `rule task.update_budget` checked for monotone cost. Handle reuse: double resume of one continuation, resume of a continuation a newer one superseded, a completed task on every observing operation twice, double cancel, RFC 0027 X2 byte-identity against a look-alike daemon, and both epoch predicates. **The campaign falsified two of the three conjuncts and the fixes closed them.** Seventeen attacks held; five landed, on three daemon-layer defects, none of them in `continuum-workspace`'s snapshot/lineage layer — (1) `workspace.create` named a lineage by the content identity of the snapshot it created and inserted it unconditionally, so an identical create under a fresh idempotency key rewound the `Fork` to its origin: the superseded snapshot was served again by `verification.start` and `task.resume`, the snapshot that was the head was stranded as `CapabilityDenied`, and an unsealed re-create revoked a live continuation (three tests) — **fixed by bn-n1xou**, which makes an identical create converge (put-if-absent on the lineage, the held record untouched, `sealed` monotone so no create un-seals), the G0-DX-13 disposition for identical creation read at the lineage; (2) `task.resume` applied the request's budget before it tested `is_terminal`, so it wrote a cancelled task's budget that `task.update_budget` refuses to write and its documented no-op was not one (one test) — **fixed by bn-10093**, which moved the terminal check above the ledger write so the two operations produce one observable for one ceiling on one terminal task; (3) `daemon::state::ReplayKey` omitted `RequestEnvelope.budget`, which is in the `task_*` preimage, so one key answered two different canonical requests with one campaign instead of `IdempotencyKeyReused` (one test) — **fixed by bn-h1zqz**, which put the whole canonical request in the key (`budget`, `output_policy`, `page`) with the field-by-field audit of what stays out, and why, recorded at the type. All five reproductions are the same wire sequences with flipped assertions, and all five run un-ignored in the default suite as regression guards | **falsification-and-fix cycle complete.** The idempotence and stale-snapshot-rejection conjuncts were falsified against the production implementation by bn-1kp6's campaign, which held `src/` frozen (the bn-21dd precedent), so the evidence that the three defects were real was produced with no opportunity to shape the implementation around them; they were repaired by bn-n1xou (convergent `workspace.create`), bn-10093 (`task.resume`'s terminal check above its budget write), and bn-h1zqz (`ReplayKey` covers the canonical request), each fix landing with the campaign's own reproduction flipped into its regression guard. Deterministic resume held on every axis attacked and needed no repair. Status upgraded on the campaign's own evidence: all 22 attacks now pass against `Daemon::dispatch`, and the five that pinned defects are what hold this Status up. Wire visibility audited per fix and stated: byte-identical for every existing flow except one — a `seal: false` re-create of an already-sealed snapshot now reports the `sealed: true` its `required` response field was declared for, instead of un-sealing the record to make its own answer true. A declared field plus governing text is conformance, so no RFC 0026 flag was raised (the bn-23j7s precedent). Scope stated plainly: one thread, one request at a time (`Daemon::dispatch` takes `&mut self`), no disk and no crash injection — concurrency is G0-DX-13's axis and durability is PR 6+ per docs/35. Two concerns remain recorded without being defects: `LineageError::Unknown` maps to `CapabilityDenied` in the `workspace` family and to `StaleSnapshot` in `task`/`verification`, and `Continuation::bounds`/`frontier` are pinned at creation and read by nothing on the resume path, which is why an older continuation cannot be refused as a second meaning |
| G0-DX-04 | Can an exact counterexample be debugged causally? | branch before `Ack`/`Sync` race | enabled frontier and alternate branch reproduce expected safe/failing outcomes | DAP design remains experimental | Re-homed → G4 (Phase B) | spikes/R3_SPIKE_REPORT.md §6 (causal debugger) | artifact shape adopted from the Phase A spike; engine evidence runs as the G4 debugger gate per plan §22 |
| G0-DX-05 | Does incrementality preserve clean semantics? | mutate property, model action, source function, proof lemma, docs | invalidation cone matches declared dependency classes; incremental result equals clean result | disable affected reuse class | Re-homed → G5 (Phase C) | spikes/R3_SPIKE_REPORT.md §5 (incremental parity) | artifact shape adopted from the Phase A spike; engine evidence runs as the G5 Incremental Parity Audit per plan §22 |
| G0-DX-06 | Can a repair transaction resist overfitting? | patch exact trace only, then run neighborhood/mutation campaign | narrow repair rejected; semantic repair promoted | no autonomous promotion | Re-homed → G4 (Phase B, PR 21) | none | runs as the G4 neighborhood/mutation campaign per plan §22 |
| G0-DX-07 | Can Forge synthesize under non-vacuity? | grammar of acknowledgement guards with safety + progress | returns `synced`, rejects `false`/never-ack | redesign objectives/CEGIS loop | Re-homed → G7 (Phase E) | spikes/R3_SPIKE_REPORT.md §4 (Forge CEGIS) | artifact shape adopted from the Phase A spike; engine evidence runs as the G7 CEGIS/non-vacuity gate per plan §22 |
| G0-DX-08 | Can model/program sync admit ambiguity? | concrete field maps to multiple abstract summaries | returns typed conflict/obligations; never silently chooses | remove bidirectional editing | Re-homed → G6 (Phase D) | spikes/R3_SPIKE_REPORT.md §7 (lens conflict) | artifact shape adopted from the Phase A spike; engine evidence runs as the G6 correspondence gate per plan §22 |
| G0-DX-09 | Can humans understand explanations better than raw traces? | task-based study with experienced Rust engineers | higher diagnosis accuracy and lower time; no assurance miscalibration | redesign explanation UX | Re-homed → G8 (preregistered study per plan §21.1) | none | preregistered G8 study; preregistration requirements in header |
| G0-DX-10 | Can agents use the protocol more effectively than CLI scraping? | same benchmark with native API vs shell output | higher success, lower tokens, fewer invalid actions | simplify/rework ACI | Open — freeze-blocking (Phase A) | none | pending PR 10 |
| G0-DX-11 | Can proof service isolate versions and requests? | concurrent Lean epochs, malicious inputs, cancellation | deterministic isolated result and resource bounds | no remote proof lane | Re-homed → G6 (Phase D, PR 28) | none | runs as the G6 proof-service isolation gate per plan §22 |
| G0-DX-12 | Can intent remain stable across swarm work? | planner, repairer, prover, reviewer with adversarial patch | evidence graph catches every unauthorized intent edge | block multi-agent autonomy | Evidence (finite spike) | spikes/R3_SPIKE_REPORT.md §8 (evidence graph) | adopt artifact shape; authority table only, not enforcement (docs/53) |
| G0-DX-13 | Does content addressing survive concurrency? | concurrent identical task creation and artifact publication | one semantic identity; no lost receipts; deterministic result | redesign CAS/task transaction | Evidence (reference implementation) | crates/continuum-workspace/src/publication.rs (DX-13 exit test: 16 writers × 8 rounds → one identity, 16 receipts, schedule-independent store) and crates/continuum-value/src/identity.rs (collision-injection under adversarial hashers), adversarially re-tested by spikes/R3_SPIKE_REPORT.md §9 (falsification campaign, bn-21dd: 12 attacks at up to 48 threads — contention, abandoned/crashed publications, refusals injected at every phase, colliding-identity contention, reader/fsck races, idempotent replay — held; a 5-mutation matrix in crates/continuum-workspace/tests/dx13_mutation_campaign.rs shows the checks are load-bearing; **concurrent garbage collection against an in-flight publication FAILED the pass condition** — three reproductions retained in crates/continuum-workspace/tests/dx13_falsification.rs, fixed by bn-2siid and now running un-ignored in the default suite) | falsification-and-fix cycle complete: the pass condition was falsified by the GC/publication race (bn-21dd — collect_garbage rooted on the index alone, omitting the in-flight publications and receipts docs/35 names as roots) and repaired by bn-2siid, which pins a publication's identity as a root from its content commit until its index commit; receipts needed no root of their own, being appended in the same critical section as the index entry naming them. All three reproductions now run un-ignored in the default suite as regression guards, so the campaign's own evidence is what holds this Status up; semantics otherwise fixed in typed code (bn-210, bn-277). **Daemon-scale crash injection is delivered (bn-3dr)** and this row's deferral now names only the half that is still open. `crates/continuumd/tests/g1_crash_recovery_evidence.rs` composes the two seams rather than re-proving either: whole daemons are killed at all nine boundaries of the eight-step `Daemon::dispatch` pipeline, and the INV-017 instant *inside* one of those dispatches is reached through this crate's own `StorageFaults`, so the crash between the content commit and the index commit that docs/35's acceptance list names is injected in a real daemon's publication and the fsck is run by a *restart* rather than by the process that wrote the store. What it establishes at daemon scale: the durable image is a step function of the dispatch boundary (steps 1–7 write no store — a claim the nine-boundary sweep can falsify, not a comment); a crash between the two commits leaves residue and never a stale index entry; a composite artifact — a sealed workspace's N records, children before parents — is absent under its own name when caught part-way, and a retry converges on every record that landed, residue included; and the reconciliation is a typed byte-stable report, equal across two recoveries of one crashed state and across two independently crashed daemons. Its checks are shown load-bearing by an identity-seam mutant (a drifting `ContentIdentifier` flips the verdict to `defective` where the faithful control is `clean`) and by two withheld-mirror controls. **Daemon-scale linearizability remains open and is not this bone's:** `Daemon::dispatch` takes `&mut self` and serves one request at a time, so there is no concurrent daemon to linearize yet — the store's own concurrency is what the 48-thread campaign above covers, and the daemon-scale question arrives with the concurrent daemon, per docs/35 |
| G0-DX-14 | Can cancellation of verification work close correctly? | cancel DPOR, solver, proof, and synthesis tasks at every phase | no leaked obligations; resumable artifacts either committed or absent | integrate deeper with asupersync | Evidence (reference implementation) | `crates/continuum-task/tests/dx14_cancellation_matrix.rs` (the four shapes as publication/suspension profiles over the region+budget calculus: 27 instrumented phases × 4 placements × 3 teardown arrivals = 324 matrix runs holding the declared outcome table; a 108-run request window in which work keeps stepping past the request; a 27-row check that a cancelled scope admits no further worker or sub-scope; and a four-lane portfolio swept over all 630 interleavings of its suffixes with the cancellation completed at each of 8 positions = 5,040 runs — every run checked for a balanced obligation ledger, no artifact left neither committed nor absent, agreement with `EvidenceBook::reconcile`'s independent budget accounting, and RFC 0026's both-or-neither on every arm) and `crates/continuumd/tests/dx14_cancellation_matrix.rs` (the same four shapes as dispatch programs: 10 phases through `Daemon::dispatch` plus the faulted-dispatch and unheld-task phases, each asserting the wire's nullable `continuation` is present exactly on the arm the region layer put the answer on, and `TaskRegions::is_total` after every one). Load-bearing-ness shown by mutation: removing the drain's discard, reporting a non-resumable lane as resumable, accepting a merely-quiescent worker at finalize, dropping the cancel scope's committed evidence, and leaking a scope on the fault path are each caught by several of these tests | pass condition met over the region+budget calculus (bn-2gk, bn-1gc) and the daemon rewiring (bn-12t), by the campaign in bn-2zy, with no change to the system under test. Scope stated plainly: three of the four engines do not exist yet, so the lanes are lifecycle **profiles** rather than engines — `CancelOutcome` is a function of committed evidence and declared resumability alone, so a profile presents everything the rule can see, and a real DPOR/solver/proof/synthesis engine is held to this table rather than to a new one. Cancellation arriving *mid-publication* is proved at the calculus grain only: `Daemon::dispatch` holds `&mut DaemonState` for a whole call, so no wire cancel can land between a stage and its commit. Asupersync integration remains Phase B (PR 14) per plan §21. Adversarially re-tested by bn-3p32 (`crates/continuumd/tests/dx14_falsification.rs` + `crates/continuum-task/tests/dx14_falsification_calculus.rs`, 18 attacks: hostile post-cancel wire sequences, two readings of resumability over 8 residual profiles, the cancel-arm no-constructor claim as a wire property, repeat-cancel byte-identity, adversarial B18 paths, cross-daemon and stale continuation replay, 120 orderings of a five-operation hostile suffix, 216 hostile three-operation calculus programs mid-publication with EvidenceBook reconciliation, composed-refusal byte-identity, two-fresh-daemons determinism): both named conjuncts held under all 18, but A1 **falsified record closure** — `task.resume` applies a request budget before its terminal check, so cancel → resume-with-budget rewrites a Cancelled task's ceilings and can append `task.budget_suspended` to a closed record (pinned as FALSIFICATION in the campaign file; A2 is the localising control). **Falsification-and-fix cycle complete:** bn-10093 moved `task.resume`'s terminal check above its ledger write, so a resume's budget arm produces `task.update_budget`'s own observable for a terminal task — nothing written, no milestone, no event — and the cancelled record is byte-identical across `cancel` → `resume(continuation, budget)`. A1 now runs un-ignored in the default suite as a regression guard asserting exactly that, in A2's shape, with A2 unchanged beside it; bn-1kp6's attack 18 guards the same fix from the G0-DX-03 side. The disposition on the continuation table is recorded in code where the table lives: a terminal task's continuations are kept, guarded by the refusal rather than pruned, because a `cont_*` is a durable artifact identity valid within a protocol major (RFC 0026) and pruning would answer a resume with RFC 0027 X2's undistinguished denial instead of the honest terminal status |
| G0-DX-15 | Can the corpus generate agent tasks without leakage? | hidden mutation splits and source-hash isolation | no answer leakage; deterministic grading; held-out behavior variants | benchmark invalid | Re-homed → G9 (corpus/benchmark program) | none | owned by the G9 corpus/benchmark program per plan §22 |

## Promotion rule

A spike may become architecture only when it has:

1. a precise semantic claim;
2. a reference implementation or checker;
3. adversarial mutations;
4. deterministic artifacts;
5. a baseline;
6. a failure policy;
7. a path to independent evidence.

A visually impressive demo is not a pass condition.
